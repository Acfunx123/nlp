1. Train a model:
python main.py

2. test a model:
python run.py image_filepath

3. network structure:
structure located in folder: runs

run command "tensorboard --logdir runs --port 10112" can see the structure
